language: ruby

sudo: false

notifications:
  email:
    on_success: change # default: always
    on_failure: change # default: always

rvm:
  - 2.2

install:
  - bundle install

script: bundle exec jekyll build
